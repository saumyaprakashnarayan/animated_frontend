# Frontend Requirements
# The frontend is built using Vite and requires Node.js.
# To run the frontend locally:
# 1. Install Node.js
# 2. Run `npm install`
# 3. Run `npm run dev`

# Backend Requirements
# This project uses a FastAPI backend (server.py) to handle API requests.
fastapi==0.110.0
uvicorn==0.27.1
sqlalchemy==2.0.28
pydantic==2.6.3
python-dotenv==1.0.1
psycopg2-binary==2.9.9
