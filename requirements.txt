# This project uses a custom Python backend (server.py) to handle API requests and serve static files.
# The backend relies exclusively on Python standard libraries:
# - http.server
# - sqlite3
# - json
# - urllib
#
# No external pip dependencies are required. 
#
# To run the project locally, execute:
# python3 server.py
