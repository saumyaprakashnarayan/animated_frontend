import React from 'react';

const WorkSection = () => {
  return (
    <section id="work" style={{ padding: '8rem 5%' }}>
      <h2 className="section-title">Featured Deployments</h2>
      <p className="section-subtitle">Real-world performance at enterprise scale.</p>
    </section>
  );
};

export default WorkSection;
